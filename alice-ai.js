/**
 * Веб-сервис: сравнение ответов нескольких ИИ и извлечение ссылок.
 * Запуск: npm start  →  http://localhost:3847
 *
 * Ключи — только в .env (см. .env.example). Старый однофайловый скрипт перенесён в server + src/.
 */
import "./server.js";
